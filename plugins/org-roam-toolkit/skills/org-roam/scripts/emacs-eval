#!/bin/bash
# Forwarder to ortk-emacs-eval (installed by Homebrew), pre-applied with
# --pkg=org-roam-skill so callers can write plain elisp expressions.
exec ortk-emacs-eval --pkg=org-roam-skill "$@"
